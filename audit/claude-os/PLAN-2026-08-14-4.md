# Plan gaudit — claude-os — 2026-08-14 (4e cycle du jour, 6e tour de rotation)

## Contexte

Premier repo du 6e tour de rotation (les 4 repos ex æquo à `dernier_audit_termine =
2026-08-14`, un tour complet n'est pas une condition d'arrêt — §6/§7). Aucune nouvelle
consigne Asana.

## Diagnostic

`bash scripts/verify-dna-consistency.sh` : les 5 checks passent, 0 item Tier 1.

## R&D (étape dédiée, cf. SKILL.md §3)

Constat concret plutôt que spéculatif : le mode de défaillance documenté le 2026-08-10
(session interrompue entre le merge de la PR cible + les commentaires Asana de clôture
et la persistance `claude-os`) vient de se reproduire une 2e fois **ce jour même** (cycle
`general` 3e, reconstruit en tout début de ce cycle de rotation — cf. `audit/STATE.md`
§reconciliation 2026-08-14). §5bis existait déjà côté *prévention* (vérifier le push
avant le commentaire Asana), mais rien ne formalisait la *détection* systématique par le
cycle suivant — je l'ai fait par raisonnement ad hoc cette fois, ce qui n'est pas
reproductible de façon fiable session après session (mémoire zéro entre cycles).

## Items

### Tier 1 — correctif sûr, fort impact

- [x] T1.1 — `SKILL.md` §2 : formaliser la comparaison horodatage Asana vs `STATE.md` en
  tout début de cycle, comme geste systématique et non un raisonnement à reconstruire à
  chaque récidive.

## Verdict

1 item Tier 1 exécuté (durcissement doctrine, pas de bug applicatif — ce repo n'a pas de
code au sens classique). Pas d'autre item Tier 2/3 identifié ce cycle au-delà de celui-ci
— la R&D de ce cycle a porté spécifiquement sur l'incident du jour plutôt que sur une
exploration large (déjà faite au cycle précédent sur ce repo, cf.
`PLAN-2026-08-14-3.md`).
