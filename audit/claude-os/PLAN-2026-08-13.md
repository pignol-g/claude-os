# Plan gaudit — claude-os — 2026-08-13

## Diagnostic

Repo statique depuis le cycle du 2026-08-10 (PR #40, dernier commit `ca0b543`) — aucun commit
humain ni session parallèle entretemps (`git log`/`git status` clean au démarrage de ce cycle).
Les 4 repos sont ex æquo à `dernier_audit_termine = 2026-08-10` dans `audit/STATE.md` ; par ordre
de table, `claude-os` est pris comme cible.

Lecture de `CLAUDE.md`, `REPRISE.md`, `INBOX-QUESTIONS.md`, `CLAUDE-DNA-CC-CORE.md` (v3.3),
`.claude/hooks/session-start.sh`, `.claude/settings.json`, `README.md`, `scripts/`,
`audit/STATE.md`. Aucun bug fonctionnel trouvé côté hook/skills/CORE. Un point de dette
concret identifié :

- **`to-chat/` bookkeeping périmé depuis le bump du 2026-08-07** — le cycle `gaudit` du
  2026-08-07 (T1.2, commit `9f57b1a`) a bumpé `CLAUDE-DNA-CHAT.md` de v2.1 à **v2.2**
  (réplication du trigger `gprompt` manquant côté Chat), mais **aucun** des 3 fichiers de
  suivi `to-chat/` n'a été mis à jour en conséquence : `_upload-status.json` affiche encore
  `current_version: v2.1 / current_date: 2026-07-22`, `_TODO.md` référence encore v2.1, et
  `_track-log.md` n'a pas de ligne pour le bump v2.2. Trois cycles `gaudit` se sont enchaînés
  depuis (2026-08-09, 2026-08-10 ×2) sans que cette dérive ne soit corrigée — ces fichiers sont
  pourtant la source machine-lisible que le hook `session-start.sh` lit pour signaler les
  uploads Chat en attente (`§3` du hook) ; rester sur `v2.1` sous-représente ce qui reste à
  uploader (le hook affichera bien `pending: true` mais le TODO humain reste factuellement faux
  sur la version réellement en attente). Item Tier 1 : correctif mécanique, sûr, sans ambiguïté.

Rien d'autre de concret trouvé à ce passage (REF §historique en retard sur CORE reste une
dérive déjà évaluée cosmétique lors des cycles précédents — pas de nouvel élément la
justifiant, non repris ici).

## Tier 1 — correctifs sûrs

- [x] T1.1 — `to-chat/_upload-status.json` : `current_version` v2.1→v2.2, `current_date`
      2026-07-22→2026-08-07, `reminders_sent` incrémenté.
- [x] T1.2 — `to-chat/_TODO.md` : item « En attente » mis à jour v2.1→v2.2 (mention du trigger
      `gprompt` ajouté côté Chat, cumule toujours les versions précédentes non uploadées).
- [x] T1.3 — `to-chat/_track-log.md` : nouvelle ligne pour le bump 2026-08-07 (CORE v3.3 + CHAT
      v2.2 — trigger `gprompt` manquant ajouté), statut « DNA-CHAT pending upload ».

## Tier 2 / Tier 3

Aucun identifié ce cycle.

## PR

https://github.com/pignol-g/claude-os/pull/41 — mergée automatiquement (pas de CI, merge propre).
