# Plan gaudit — claude-os — 2026-08-10

## Diagnostic

Repo choisi par rotation : `claude-os` et `general` ex æquo les plus anciens
(`dernier_audit_termine = 2026-08-07`) après la reconciliation de ce même cycle
(`audit/candidaturePilote/` + `audit/ClaudeAchatMaison/`, PR #36) — `claude-os` pris en premier
(première ligne de la table de rotation), et il porte lui-même le système `gaudit` dont la
lacune de fiabilité vient d'être découverte : bon repo pour la corriger à la source.

Lecture : `CLAUDE.md`, `REPRISE.md`, `INBOX-QUESTIONS.md`, `.claude/skills/gaudit/SKILL.md`,
`audit/STATE.md` (post-reconciliation), historique git récent, versions des 3 fichiers DNA.

**Constat structurant #1** (déjà détaillé dans `audit/STATE.md` §Historique 2026-08-10) : le
cycle `gaudit` du 2026-08-09 a mergé deux PR cible (candidaturePilote #166, ClaudeAchatMaison
#37) et posté ses commentaires Asana de clôture, mais son commit/push `REPORT`+`STATE.md` côté
`claude-os` n'a jamais atteint `origin/main` — `STATE.md` a menti sur l'état réel pendant 3
jours. `SKILL.md` §5 liste bien l'ordre correct (REPORT → STATE.md → commentaire Asana) mais
sans jamais imposer de **vérifier** que le push a réellement abouti avant l'étape suivante — la
panne silencieuse d'un `git push` ne bloque rien dans le flux actuel. Fix mécanique, isolé,
directement dans les instructions de la skill (Tier 1).

**Constat structurant #2** : `REPRISE.md` est de nouveau figé (dernier état écrit 2026-08-07,
alors que 2 cycles complets + la reconciliation d'aujourd'hui ont eu lieu depuis) — exactement le
même symptôme que celui corrigé en T1.3 du cycle précédent (2026-08-07), qui n'avait pourtant
pas de convention de mise à jour automatique par `gaudit` lui-même. `gaudit` ne touche
normalement pas `REPRISE.md` (mémoire de session interactive, pas un artefact `audit/`) — mais
comme il est aujourd'hui manifestement figé après plusieurs cycles `gaudit` consécutifs qui ne le
mettent jamais à jour, corriger l'état une fois de plus **et** documenter dans `REPRISE.md`
lui-même que son entretien reste hors périmètre `gaudit` (pour éviter d'attendre que la routine
le fasse à chaque passage).

**Constat structurant #3** : la section « Routine Claude Code Remote » de `SKILL.md` documentait
une incertitude (« statut de `mcp__github__*` dans ce mode non vérifié en conditions réelles »,
2026-08-07). Deux firings réels depuis (2026-08-09 et 2026-08-10, ce cycle) confirment que
`mcp__github__*` **et** `mcp__Asana__*` sont bien disponibles dans les sessions déclenchées —
preuve : les deux cycles ont créé/mergé des PR et posté des commentaires Asana avec succès.
Mettre à jour la doc pour lever l'incertitude (Tier 2 — cosmétique/doc, aucun comportement ne
change).

## Tier 1 — correctifs sûrs, fort impact

- [ ] T1.1 — `SKILL.md` §5 : ajouter une étape de vérification explicite entre le
      commit/push `REPORT`+`STATE.md` côté `claude-os` et le commentaire Asana de clôture —
      confirmer que le push a atteint `origin` (ex. comparer le SHA local à `origin/<branche>`
      après push, ou tout mécanisme équivalent) avant de poster quoi que ce soit sur Asana.
      Documente aussi la cause du 2026-08-09 comme exemple concret dans la skill elle-même
      (pas seulement dans `STATE.md`, pour qu'un futur cycle qui ne relit pas tout l'historique
      de `STATE.md` voie quand même la règle).
- [ ] T1.2 — Rafraîchir `REPRISE.md` : état réel courant (versions DNA inchangées depuis
      2026-08-07 : CORE v3.3, Chat v2.2, REF v2.1 ; les 4 repos ont désormais tous un
      `dernier_audit_termine` ; lacune de fiabilité 2026-08-09 découverte et corrigée
      2026-08-10 ; disponibilité MCP GitHub/Asana en Routine confirmée). Ajouter une note
      explicite : `REPRISE.md` reste hors périmètre d'entretien automatique de `gaudit` (mémoire
      de session interactive, pas un artefact `audit/`) — ne pas attendre qu'un cycle `gaudit`
      le rafraîchisse spontanément à chaque passage.

## Tier 2 — améliorations

- [ ] T2.1 — `SKILL.md` §"Routine Claude Code Remote" : remplacer le paragraphe d'incertitude
      sur la disponibilité de `mcp__github__*`/`mcp__Asana__*` par une confirmation, en citant
      les deux firings réels (2026-08-09, 2026-08-10) comme preuve. Ne change aucun
      comportement — documentation uniquement.

## Tier 3 — R&D / reporté (pas exécuté ce cycle)

- **Réactivation du trigger `trig_01Y6mLjE6VSXYg8WtmdPA9aB`** : la confirmation T2.1 lève
  l'obstacle technique documenté, mais l'activation/désactivation de la Routine reste une
  décision de Guillaume (cf. `SKILL.md` §Routine, « décision de Guillaume ») — non exécutée par
  ce cycle, signalée dans `REPRISE.md` (T1.2) comme option de reprise.
- **`CLAUDE-DNA-CC-REF.md` table historique arrêtée à v2.1** alors que le CORE est à v3.3 :
  déjà évalué au cycle précédent (2026-08-07) comme cosmétique, aucun risque fonctionnel — pas
  rouvert sans nouvel élément.
- **`INBOX-QUESTIONS.md` item 2026-07-21** (candidaturePilote, Sonnet vs Opus + migration profil
  rédactionnel) : décision Guillaume requise, hors périmètre d'exécution autonome.

## Pull request

_(à compléter en fin de plan — pas de CI sur ce repo, carve-out merge auto applicable si merge
propre.)_
