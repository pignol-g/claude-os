# Rapport gaudit — claude-os — 2026-08-07

## Période couverte

2026-08-07 (cycle unique, ~1h) — premier cycle réel de la routine `gaudit`, immédiatement après
le merge de PR #33 qui a landé le système lui-même.

## Items traités

| Item | Diagnostic | Action | Résultat |
|---|---|---|---|
| T1.1 | Skill `gprompt` existante mais sans section trigger dans `CLAUDE-DNA-CC-CORE.md`, alors que `CLAUDE.md` la liste comme un des skills implémentant les triggers du CORE. | Ajout d'une section "Combo prompt — trigger `gprompt`" mirroir des autres (gpose/gauto/gaudit/grech/gtri). Bump v3.2 → v3.3. | Fait, commité, mergé. |
| T1.2 | Même trou côté `CLAUDE-DNA-CHAT.md` (gprompt ne dépend d'aucune capacité fichier/git, portable côté Chat). | Section équivalente ajoutée + entrée historique. Bump v2.1 → v2.2. | Fait, commité, mergé. |
| T1.3 | `REPRISE.md` figé depuis 2026-05-23 (v2.1) malgré des dizaines de commits depuis (gtri, module Asana, gprompt, gaudit...) — violation de la propre convention du repo. | Réécriture complète avec l'état réel courant (versions DNA, skills récentes, questions ouvertes actualisées). | Fait, commité, mergé. |
| T2.1 | `README.md` racine = stub `# claude-os` alors que le repo est public. | Résumé condensé de `CLAUDE.md` §"Ce que ce repo est" ajouté. | Fait, commité, mergé. |
| T2.2 | Références obsolètes à `CLAUDE-DNA-CC.md` (pré-split) dans `CLAUDE-DNA-CHAT.md` (2 endroits : commentaire d'en-tête + section Référence). | Pointées vers `CLAUDE-DNA-CC-CORE.md`. | Fait, commité, mergé. |
| T2.3 | Stubs legacy `CLAUDE-DNA-CC.md` / `CLAUDE-DNA.md` : condition de retrait documentée (tous les hooks clients migrés vers `DNA_URL=…CORE.md`) vérifiée remplie (grep sur `candidaturePilote` + `ClaudeAchatMaison`). | Fichiers supprimés, `CLAUDE.md` et `CLAUDE-DNA-CC-REF.md` (§dna-pointe) mis à jour en conséquence. | Fait, commité, mergé. |

## Pull request

https://github.com/pignol-g/claude-os/pull/34 — **mergée automatiquement** (carve-out `gaudit` :
pas de CI configurée sur ce repo → condition considérée satisfaite par défaut ; `mergeable_state:
clean`, aucun conflit).

## Items reportés au prochain cycle

- Aucun item du plan de ce cycle n'a été reporté — Tier 1 et Tier 2 complets.

## Risques / dette identifiés non traités (Tier 3)

- **`candidaturePilote/.claude/hooks/session-start.sh` encore en v2.0** : pas de `git pull
  --rebase --autostash origin main` au démarrage (contrairement à claude-os et ClaudeAchatMaison,
  passés en v2.1), pas de garde once-per-session, encore nommage `from-cc/` uniquement. Risque
  réel : le CORE documente lui-même un incident (27/05/2026, sur un autre projet) où l'absence de
  pull au démarrage a fait travailler une session sur un état git périmé, causant des conflits
  massifs au push. Hors périmètre de ce cycle (repo cible = claude-os). **Recommandation** :
  prioriser `candidaturePilote` au prochain cycle `gaudit`, ou le signaler à Guillaume avant si
  jugé urgent.
- `INBOX-QUESTIONS.md` (item du 2026-07-21, correctifs `candidaturePilote` Sonnet-vs-Opus +
  migration profil rédactionnel) : nécessite une décision explicite de Guillaume, hors périmètre
  d'exécution autonome de `gaudit`.
- `CLAUDE-DNA-CC-REF.md` : table historique arrêtée à v2.1 (2026-06-13) alors que le CORE est
  maintenant à v3.3. Cosmétique, aucune règle documentée n'impose sa mise à jour à chaque bump
  CORE, aucun risque fonctionnel identifié.

## Prochaine cible de rotation prévue

Rotation par staleness : `general`, `candidaturePilote` et `ClaudeAchatMaison` sont tous les
trois encore à `jamais` (claude-os vient d'être marqué comme audité aujourd'hui). Le prochain
cycle prendra le premier de cette liste par ordre de la table de rotation (`general`), sauf
nouvelle consigne Asana entretemps ou décision explicite de prioriser `candidaturePilote` au vu
du risque hook signalé ci-dessus.
