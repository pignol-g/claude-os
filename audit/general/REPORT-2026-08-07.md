# Rapport gaudit — general — 2026-08-07

## Période couverte

2026-08-07 (cycle unique, court) — deuxième cycle réel de la routine `gaudit`, à la suite du
cycle `claude-os` du même jour.

## Items traités

| Item | Diagnostic | Action | Résultat |
|---|---|---|---|
| T2.1 | `CLAUDE.md`/`README.md` ne documentent que l'en-tête de provenance `gtri` (claude-os → general). Un fichier réel (`perso/RECHERCHE_fille-au-pair_2026-07-22.md`, origine Asana) utilise un format différent sans convention écrite — gap pour un repo "géré par IA indifféremment laquelle". | Note ajoutée dans `CLAUDE.md` §Alimentation et `README.md` §Traçabilité couvrant le cas de dépôt direct hors `gtri`. | Fait, commité, mergé. |

Aucun item Tier 1 : pas de bug actif identifié sur ce repo (petit, récent, cohérent).

## Pull request

https://github.com/pignol-g/general/pull/5 — **mergée automatiquement** (carve-out `gaudit` :
pas de CI sur ce repo, purement Markdown → condition satisfaite par défaut ; `mergeable_state:
clean`, aucun conflit).

## Items reportés au prochain cycle

- Aucun item du plan de ce cycle n'a été reporté.

## Risques / dette identifiés non traités (Tier 3)

- **`perso/profil-redactionnel-guillaume.md` reste un stub vide** (v0.1, 2026-06-13) alors que le
  contenu réel (255 lignes, voix FR+EN) existe déjà dans
  `candidaturePilote/livrables/lettres/_templates/profil-redactionnel-guillaume.md`. Le stub
  lui-même invite à copier ce contenu. **Non traité intentionnellement** : la migration
  soulève une question architecturale déjà identifiée et non tranchée par Guillaume
  (`claude-os/INBOX-QUESTIONS.md`, 2026-07-21, `R-002`) — copier sans garantie anti-divergence
  créerait deux sources de vérité pour la même donnée (contraire à la règle "un seul
  propriétaire" du repo). `gaudit` ne prend pas cette décision à la place de Guillaume.

## Prochaine cible de rotation prévue

`candidaturePilote` — repo avec le risque le plus concret déjà identifié au cycle précédent
(hook `session-start.sh` en v2.0, sans git pull auto au démarrage). `ClaudeAchatMaison` reste
en réserve (`jamais`) pour le cycle suivant.
