# Plan gaudit — candidaturePilote — 2026-08-13

## Diagnostic

Repo très actif côté automatisation (veille 2×/jour, cron GitHub) mais **complètement à
l'arrêt côté session interactive/gauto depuis le 2026-07-24** (`REPRISE.md`/`TODO.md` figés
depuis le 21/07, dernier commit hors auto-veille le 07-24). Hook `session-start.sh` v2.1
(guard once-per-session) toujours en place et fonctionnel. Bookkeeping `from-cc/` cohérent
avec le contenu réel (pas de dérive comme celle trouvée sur `claude-os` ce même jour).

**Constat majeur** : **35 pull requests ouvertes**, aucune créée après le 2026-07-24 — la
totalité de l'activité de développement s'est arrêtée net il y a 3 semaines. La plus récente,
**PR #133** (« Revue consolidée des 42 PR — clôture en masse », 2026-07-24), est elle-même une
**4ᵉ tentative** de diagnostiquer ce même embouteillage (précédée par #64 le 02/07, #71 le
03/07, #128/#130/#132 le 24/07 plus tôt) — elle documente une triage complet et chiffré
(7 MERGER / 22 CLORE / 13 ARBITRER, tous repos) mais **s'arrête volontairement à
l'analyse** (« Aucun merge/close » dans son propre contenu), recommandant explicitement de
clore les runs redondants. **Personne n'a exécuté cette recommandation depuis 3 semaines**,
et aucun cycle `gaudit` précédent (2026-08-07 → 2026-08-10, 4 passages sur ce repo) ne
l'avait re-détectée — leurs diagnostics n'avaient pas vérifié l'état des PR ouvertes.

Vérification fraîcheur : les 17 PR `candidaturePilote` de la liste 🗑️ CLORE de #133 sont
toujours ouvertes, sans commit ni activité depuis leur création — l'analyse reste valide
telle quelle pour cette sous-liste (contrairement à la liste MERGER, dont le contenu
`sources.yaml` a très probablement divergé des ~40 commits auto-veille quotidiens depuis).

## Tier 1 — correctifs sûrs et à fort impact

- [x] T1.1 — Fermer les 17 PR `candidaturePilote` de la liste 🗑️ CLORE de #133 (#30, #35,
      #43, #60, #62, #64, #68, #71, #72, #77, #80, #83, #90, #93, #95, #128, #130), chacune
      avec un commentaire de renvoi. Fermeture ≠ suppression (branches conservées, pas de
      `branch -D`/delete remote — safety interdit respecté), réversible par réouverture.
      Action distincte du carve-out merge de PR (le DNA ne restreint que le **merge** sans
      validation, pas la fermeture sans merge).
- [x] T1.2 — Commentaire de suivi sur #133 documentant l'exécution partielle + ce qui reste
      hors périmètre autonome.

## Tier 2 — signalé, pas exécuté (nécessite vérification fraîche ou décision Guillaume)

- Chaîne **MERGER** `candidaturePilote` #40→#46→#67→#125 (`sources.yaml`) — mergeabilité à
  revérifier avant toute action, contenu potentiellement obsolète.
- **ARBITRER** `candidaturePilote` #48, #53, #58, #61, #66, #74, #98, #129, #132 — décision
  de contenu, hors périmètre autonome.
- 4 PR jamais couvertes par l'analyse d'origine : #25, #27, #29, #134.
- Volet `claude-os` (#13, #16, #17, #18, #19, #22) et `ClaudeAchatMaison` (#30, #31) de la
  même revue #133 — hors périmètre de ce cycle (un repo à la fois), à traiter lors d'un
  futur cycle `gaudit` sur ces repos.
- **116 issues GitHub ouvertes** (`Veille pilote — AAAA-MM-JJ — N nouveauté(s)`), créées
  2×/jour par le workflow `veille.yml` sans mécanisme de clôture nulle part (ni le workflow,
  ni la skill `analyse-offre`). Pas assez de certitude sur ce qui a été réellement revu par
  Guillaume pour fermer en masse sans risque de perdre le suivi — signalé pour décision
  (ex. auto-clore après N jours, ou lier la clôture au traitement `analyse-offre`).

## Tier 3

Rien de nouveau — items déjà connus (backlog moteur de veille → hors périmètre, rôle
`audit-veille` ; migration `from-cc`/`from-chat` → `to-*` ; `INBOX-QUESTIONS.md` R-002/R-003)
inchangés depuis le cycle du 2026-08-10.

## PR

Aucune côté `candidaturePilote` — travail de ce cycle entièrement côté GitHub (fermeture de
PR + commentaires), pas de modification de fichiers versionnés dans le repo.
