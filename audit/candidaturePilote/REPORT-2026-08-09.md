# Rapport gaudit — candidaturePilote — 2026-08-09

## Période couverte
2026-08-07 (diagnostic + démarrage du plan) → 2026-08-09 (reprise et clôture)

## Items traités

| Item | Diagnostic | Action | Résultat |
|---|---|---|---|
| T1.1 | Hook `session-start.sh` encore en v2.0 (pas de `git pull` auto au démarrage) — risque documenté par un incident réel du 27/05/2026 sur un autre projet, signalé au cycle `claude-os` du 2026-08-07. | Mise à jour v2.0 → v2.1 : ajout du `git pull --rebase --autostash origin main` obligatoire + garde once-per-session, sur le modèle exact du hook `ClaudeAchatMaison`. Aucun renommage de dossier. | Fait, commité, syntaxe shell vérifiée (`bash -n`). |
| T1.2 | `from-chat/` contenait 5 PDF de vraies LM 2024 de Guillaume (PII : nom, adresse, téléphone, photo), déposés le 2026-07-15, jamais nettoyés après intégration. Vérification (lecture intégrale des 5 PDF) : squelette identique déjà documenté dans `profil-redactionnel-guillaume.md` §8b, y compris pour Oyonnair et Alpine Airlines non citées nommément en §8b. | Suppression des 5 PDF (`git rm`). | Fait, commité. |
| T2.1 | Sans trace de la vérification T1.2, une session future pourrait se redemander si ces fichiers restaient à traiter. | Entrée journal §11 dans `profil-redactionnel-guillaume.md` documentant la vérification et la suppression. | Fait, commité. |

## Pull request

https://github.com/pignol-g/candidaturePilote/pull/166 — **mergée automatiquement** (squash). CI (`veille.yml`) non déclenchée sur cette PR — trigger `pull_request` filtré sur `scripts/veille/**`/`.github/workflows/veille.yml`, aucun des deux touché par ce diff (0 check run). `mergeable_state: clean`, pas de conflit. Carve-out `gaudit` appliqué.

## Items reportés au prochain cycle

- **Migration légère `from-cc/`/`from-chat/`/`to-claude-os/` → `to-chat/`/`to-cc/`/`to-os/`** — détectée mais non exécutée (règle DNA « pas de migration lourde silencieuse, `gpose` et demande d'abord »). À proposer à Guillaume en session interactive (`migrate-projet`, niveaux `migA`/`migB`/`migC`).
- **`from-chat/SKILL-1.md`** — provenance peu claire (définition d'une skill `advanced-research` générique sans rapport apparent avec `candidaturePilote`, ne suit pas le format d'export de session Chat attendu). Non touché, signalé pour arbitrage Guillaume.
- **Backlog moteur de veille** (`TODO.md` §"Moteur de veille — backlog audit") — hors périmètre de `gaudit`, relève de la skill dédiée `audit-veille` de ce même repo.

## Risques / dette identifiés non traités

- **`INBOX-QUESTIONS.md` (claude-os, 2026-07-21)** et **`to-claude-os/REMONTEES-OS.md`** (R-002/R-003, en attente depuis le 2026-06-13) — décisions Guillaume déjà identifiées, non tranchées par ce cycle.

## Prochaine cible de rotation prévue

`ClaudeAchatMaison` (seul repo à `jamais` restant dans la table de rotation), sauf nouvelle consigne Asana entretemps.
