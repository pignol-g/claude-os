# Plan gaudit — candidaturePilote — 2026-08-07

## Diagnostic

Repo actif (veille automatisée tourne 2×/jour, commits quotidiens). Lecture : `CLAUDE.md`,
`REPRISE.md`, `TODO.md`, `.claude/hooks/session-start.sh`, `.claude/skills/`, `from-cc/`,
`from-chat/`, `to-claude-os/`. Repo cible choisi car un risque concret a été signalé au cycle
`claude-os` du même jour (hook `session-start.sh` encore en v2.0).

**Constat structurant** : ce repo n'a jamais été migré vers la convention `to-chat/`/`to-cc/`
(v2.5, 2026-06-13) — il utilise encore `from-cc/`/`from-chat/`/`to-claude-os/` (au lieu de
`to-os/`). `CLAUDE-DNA-CC-REF.md` §migrate-projet est explicite : **« pas de migration lourde
silencieuse — gpose et demande »**. Le renommage des dossiers n'est donc **pas** exécuté par ce
cycle (Tier 3). En revanche, le risque de sécurité concret (absence de git pull au démarrage)
est traité isolément en Tier 1 : c'est un fix mécanique, ponctuel, sans toucher à la structure
de dossiers — le hook cible v2.1 gère déjà nativement le fallback `to-chat/`↔`from-cc/`.

**Découverte en cours d'audit** : `from-chat/` contient 5 PDF de vraies LM 2024 de Guillaume
(PII : nom, adresse, téléphone, photo) déposés le 2026-07-15, jamais nettoyés. Vérification
faite (lecture des 5 PDF) : leur contenu est un **squelette identique** (même texte sur 2
paragraphes, seule la clôture change par compagnie — Twin Jet, Oyonnair, HOP!, Alpine Airlines,
Air France) et correspond **déjà** au corpus documenté en détail dans
`livrables/lettres/_templates/profil-redactionnel-guillaume.md` §8b (« analyse de 8 vraies LM
2024 », session du 2026-06-13 — mêmes tics vérifiés : sécurité citée en dernier, CVVL,
reconversion dev web santé, clôture standard). **Aucun nouveau signal stylistique** au-delà de
ce qui est déjà capturé — Oyonnair et Alpine Airlines ne sont pas nommément cités en §8b mais
suivent exactement le même squelette. Ce ne sont donc pas des livrables « en attente de
traitement » comme le laissait penser `REPRISE.md`/`TODO.md` — plutôt des sources déjà
exploitées, jamais nettoyées après coup (violation du cycle de vie éphémère documenté de
`from-chat/`/`to-cc/` : « à supprimer après intégration confirmée »).

## Tier 1 — correctifs sûrs, fort impact

- [x] T1.1 — Mettre à jour `.claude/hooks/session-start.sh` de v2.0 vers v2.1 : ajoute le
      `git pull --rebase --autostash origin main` obligatoire au démarrage (CORE §3, risque
      documenté par un incident réel sur un autre projet) + la garde once-per-session, sur le
      modèle exact du hook `ClaudeAchatMaison` (déjà en v2.1, déjà rétrocompatible
      `to-chat/`↔`from-cc/` nativement). **Ne renomme aucun dossier** — fix isolé, pas de
      migration lourde.
- [ ] T1.2 — Nettoyer `from-chat/` : supprimer les 5 PDF de LM 2024 (contenu déjà pleinement
      analysé et documenté dans `profil-redactionnel-guillaume.md` §8b, vérifié verbatim) —
      complète leur cycle de vie éphémère resté bloqué depuis le 2026-07-15 (PII qui n'a plus de
      raison de rester versionnée).

## Tier 2 — améliorations

- [ ] T2.1 — Ajouter une entrée journal (§11) à `profil-redactionnel-guillaume.md` documentant
      la vérification : les 5 PDF de `from-chat/` (dont Oyonnair et Alpine Airlines, non cités
      nommément en §8b) confirment le même squelette v1 déjà documenté, aucun enrichissement de
      fond nécessaire. Referme la boucle pour éviter qu'une session future ne se demande si ces
      fichiers restent à traiter.

## Tier 3 — R&D / reporté (pas exécuté ce cycle)

- **Migration légacy `from-cc/`→`to-chat/`, `from-chat/`→`to-cc/`, `to-claude-os/`→`to-os/`** :
  détectée, mais `CLAUDE-DNA-CC-REF.md` §migrate-projet est explicite (« règle d'or : pas de
  migration lourde silencieuse, gpose et demande d'abord »). `gaudit` ne déclenche pas cette
  migration unilatéralement — à proposer à Guillaume via `migrate-projet`/`gpose` en session
  interactive (niveaux `migA`/`migB`/`migC` déjà définis dans le REF).
- **`from-chat/SKILL-1.md`** : fichier de provenance peu claire (contenu = définition d'une
  skill `advanced-research` générique, sans rapport apparent avec candidaturePilote, ne suit pas
  le format attendu d'un export de session Chat). Non touché — signalé à Guillaume pour
  arbitrage plutôt que supprimé à l'aveugle.
- **Item `INBOX-QUESTIONS.md` (claude-os, 2026-07-21)** et **`to-claude-os/REMONTEES-OS.md`**
  (R-002/R-003 en attente depuis 2026-06-13) : décisions Guillaume déjà identifiées, non
  tranchées par ce cycle.
- **Backlog moteur de veille** (`TODO.md` §"Moteur de veille — backlog audit") : hors périmètre
  de `gaudit` — c'est le rôle de la skill dédiée `audit-veille` de ce même repo.

## Pull request

_(à compléter en fin de plan — ⚠️ ce repo a une vraie CI (`.github/workflows/veille.yml`),
contrairement à claude-os/general : vérifier le statut réel avant tout merge, ne pas supposer
la condition CI satisfaite par défaut.)_
