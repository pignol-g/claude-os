# Plan gaudit — candidaturePilote — 2026-08-14 (3e cycle du jour)

## Contexte

Cible annoncée par le cycle `general` 3e (interrompu avant exécution, cf. reconciliation
`STATE.md` 2026-08-14) — reprise comme cible réelle par ce cycle. Aucune nouvelle
consigne Asana.

Delta depuis le dernier cycle sur ce repo (`candidaturePilote` 2e cycle, HEAD `82726e8`) :
uniquement des commits automatiques `chore(veille): run auto` (2026-08-14, 2 runs) —
aucun commit humain ni autre session. Pas de conflit avec les reports du cycle précédent.

## Diagnostic

Repris à partir du rapport du 2e cycle (`REPORT-2026-08-14-2.md`) :
- Liste ARBITRER de #133 (9 PR) — inchangée, décision Guillaume toujours en attente.
- PR #29/#134 — toujours gated/draft, hors mandat autonome.
- PR #67 (Vueling) — nécessite un run `audit-veille` frais (skill dédiée), pas une action
  `gaudit` directe.
- **113 issues GitHub `veille` ouvertes sans mécanisme de clôture** — re-signalé sans
  action depuis le 08-13 (3 cycles consécutifs). Vérifié ce cycle : quasi-totalité au
  format digest quotidien strict (`Veille pilote — AAAA-MM-JJ — N nouveauté(s)`), la
  seule exception structurelle étant #11 (rapport `audit-veille`, titre différent).
  Contrairement aux items ARBITRER/#67 (arbitrage de contenu, jugement métier), celui-ci
  est un vrai déficit d'infra mécanique : aucune décision de contenu à prendre, juste
  l'absence d'un mécanisme de clôture. Repris en Tier 1 ce cycle plutôt que re-reporté
  une 4e fois (cohérent avec le correctif de doctrine du jour : agir plutôt que
  re-signaler indéfiniment un point actionnable).

## Items

### Tier 1 — correctif sûr, fort impact

- [x] T1.1 — `.github/workflows/veille.yml` : nouvelle étape qui ferme automatiquement
  les issues digest de plus de 30 jours **sans aucun commentaire** (garde de sécurité :
  toute trace d'engagement humain protège l'issue). Motif de titre strict (regex ancrée),
  n'affecte aucune autre issue labellisée `veille`. Réversible (`state_reason:
  not_planned`, réouverture manuelle possible).

## Verdict

1 item Tier 1 exécuté (mécanisme, pas un contenu métier — aucune donnée
`candidatures.json`/`cibles-compagnies.json` touchée). Items ARBITRER/#67/#29/#134
inchangés (décision Guillaume ou skill dédiée requise, hors mandat `gaudit`).
