# Plan gaudit — candidaturePilote — 2026-08-10

## Diagnostic

Rotation : `candidaturePilote` (ex æquo avec `ClaudeAchatMaison` à `2026-08-09`, premier de la
table). Lecture : `CLAUDE.md`, `TODO.md`, `REPRISE.md`, `.claude/hooks/session-start.sh` (fix du
2026-08-09 vérifié), `from-cc/`, `from-chat/`, `to-claude-os/`, historique git depuis le
2026-08-09.

**Vérification du fix T1.1 du cycle précédent** : le hook `session-start.sh` v2.1 est bien en
place et fonctionnellement correct (guard once-per-session, `git pull --rebase --autostash`
avec échec doux, chargement DNA local/curl, signal `from-cc/_upload-status.json`) — comparé
ligne à ligne au hook `ClaudeAchatMaison` (référence) : seules les sections spécifiques à ce
dernier (rappel de globalisation locale, notification via `notify.sh`) diffèrent, ce qui est
attendu et sans rapport avec le fix. Aucune régression.

**Rien de nouveau depuis le cycle du 2026-08-09** : seuls des commits automatiques `chore(veille)`
sont apparus depuis (état/digest, hors scope `gaudit` — relève de `audit-veille`). Les items
Tier 3 déjà identifiés restent dans le même état, sans élément nouveau justifiant de les
rouvrir :
- Migration légère `from-cc/`/`from-chat/`/`to-claude-os/` → convention v2.5 : toujours en
  attente d'un `gpose`/`migrate-projet` interactif avec Guillaume.
- `from-chat/SKILL-1.md` : toujours présent, toujours de provenance/intention peu claire
  (définition d'une skill `advanced-research` générique — équivalent fonctionnel de `grech`
  côté claude-os — déposée dans l'inbox d'exports Chat plutôt que dans `.claude/skills/`, sans
  suivre le format d'export attendu). Décision d'ajouter une skill est une décision de
  fonctionnalité, pas un correctif sûr — reste hors périmètre d'exécution autonome.
- `INBOX-QUESTIONS.md` (claude-os, 2026-07-21) / `to-claude-os/REMONTEES-OS.md` (R-002/R-003) :
  décisions Guillaume toujours en attente.
- Backlog moteur de veille (`TODO.md` §"Moteur de veille") : hors périmètre, rôle de
  `audit-veille`.

**`REPRISE.md`** figé au 2026-06-23 (dernière entrée en tête) — plus ancien que le hook fix du
2026-08-09, mais cohérent avec le fait qu'aucune session interactive n'a eu lieu depuis (seuls
des runs automatiques `veille` + le commit `gaudit` du 2026-08-09, qui documente déjà son propre
travail dans `claude-os/audit/`). Pas traité comme une dette ici : `REPRISE.md` reste la mémoire
de session interactive du projet, pas un artefact que `gaudit` doit maintenir à jour à chaque
passage (même position que pour `claude-os`, cf. `claude-os/REPRISE.md` §Note).

## Tier 1 — correctifs sûrs, fort impact

_(aucun item — le risque connu (hook v2.0) a déjà été corrigé au cycle du 2026-08-09, rien de
nouveau détecté ce cycle)_

## Tier 2 — améliorations

_(aucun item nouveau)_

## Tier 3 — R&D / reporté (pas exécuté ce cycle)

- Voir constats ci-dessus (migration légère, `SKILL-1.md`, `INBOX-QUESTIONS.md`/R-002/R-003,
  backlog veille) — tous déjà identifiés aux cycles précédents, aucun élément nouveau.

## Pull request

Aucune — rien à modifier ce cycle.
